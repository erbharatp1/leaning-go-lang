info:
  name: "save "
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8082/events
  body:
    type: json
    data: |-
      {
          "ID": "123456y",
          "Name": "Shivay",
          "Location": "damoh",
          "Description": "boy"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
