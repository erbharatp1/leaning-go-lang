info:
  name: /signup
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8084/signup
  body:
    type: json
    data: |-
      {
        "name": "Bageviw728@7novels.com",
        "email": "Bageviw728@7novels.com",
        "password": "Bageviw728@7novels.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
